телеграм бота -> бронь столика -> сохранение в бд -> сохраненные столики
рест апи у бота
app.get("/v2/api"

база данных
id created_at updated_at

БИблиотеки
express
postgress
body-parser
moment
uuidv4


         // if (i < 3) {
                //     t1.push({text: tabl[i].dataValues.table_name  + ' - ' +  tabl[i].dataValues.status, callback_data: `${tabl[i].dataValues.id}`})
                // }else if (i < 6 && i >= 3) {
                //     t2.push({text: tabl[i].dataValues.table_name  + ' - ' +  tabl[i].dataValues.status, callback_data: `${tabl[i].dataValues.id}`})
                // } else if (i < 9 && i >= 6) {
                //     t3.push({text: tabl[i].dataValues.table_name  + ' - ' +  tabl[i].dataValues.status, callback_data: `${tabl[i].dataValues.id}`})
                // }


                            // await bot.answerCallbackQuery({callback_query_id: msg.id})


        const mm = 'Свободен'
                    await TebleModel.create({mm})

id user_name last_name created_at updated_at


